<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/psu_widget_gray"
    android:foreground="?selectableItemBackground">


    <TextView
        android:id="@+id/favorite_widget_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:textColor="@color/black"
        android:textSize="14sp"
        app:fontFamily="@font/open_sans_medium"
        app:layout_constraintStart_toEndOf="@+id/favorite_place"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Дом" />

    <TextView
        android:id="@+id/favorite_widget_address"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="10dp"
        android:ellipsize="end"
        android:textColor="@color/psu_back_button"
        android:textSize="12sp"
        app:fontFamily="@font/open_sans_medium"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/favorite_place"
        app:layout_constraintTop_toBottomOf="@+id/favorite_widget_name"
        tools:text="ул. Мира, 45" />

    <ImageView
        android:id="@+id/favorite_place"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_favorite" />

</androidx.constraintlayout.widget.ConstraintLayout>